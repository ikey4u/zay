import Foundation

/// Keep these in sync with `project.yml` / framework Info.plists.
enum ZayBundleID {
    static let app = "dev.zay.ios"
    static let tunnel = "dev.zay.ios.tunnel"
    static let appGroup = "group.dev.zay.ios"
}
